version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21
  build:
    commands:
      - mvn clean package
  post_build:
    commands:
      - echo Build Completed Successfully
artifacts:
  files:
    - target/*.jar
  discard-paths: yes