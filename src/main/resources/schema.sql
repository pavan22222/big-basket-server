CREATE TABLE IF NOT EXISTS Items (
                                     id UUID PRIMARY KEY,
                                     name VARCHAR(255),
                                     price DOUBLE,
                                     dom DATE
    );